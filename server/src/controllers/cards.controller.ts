import { Request, Response, NextFunction } from 'express';

export const cardsController = {
  getAll: async (_req: Request, _res: Response, _next: NextFunction): Promise<void> => {
    // TODO: MVP-026
  },
  getOne: async (_req: Request, _res: Response, _next: NextFunction): Promise<void> => {
    // TODO: MVP-026
  },
};
