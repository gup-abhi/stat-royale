import { Request, Response, NextFunction } from 'express';

export const leaderboardController = {
  getPlayers: async (_req: Request, _res: Response, _next: NextFunction): Promise<void> => {
    // TODO: MVP-027
  },
};
